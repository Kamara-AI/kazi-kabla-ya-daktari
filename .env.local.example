# Copy this file to .env.local and fill in your Anthropic API key.
# NEVER commit .env.local to version control.
# The key is used server-side only in app/api/brief/route.ts.
ANTHROPIC_API_KEY=your_key_here
